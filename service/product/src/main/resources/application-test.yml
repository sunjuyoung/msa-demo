server:
  port: 9011


spring:
  application:
    name: product-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3377/product?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: rnrdj123
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    # show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: 127.0.0.1
      port: 6379


  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: product-service